Automation Layer

The automation layer is implemented as a Kubernetes operator that
watches telemetry events generated by the platform.

When anomalies are detected, the operator executes remediation
actions including pod scaling, service restarts, and infrastructure
optimization tasks.

This design enables closed-loop DevOps automation where monitoring,
analysis, and remediation operate as a unified platform.
