Observability Layer

The observability layer provides runtime visibility into Kubernetes workloads.

Metrics are collected using Prometheus and visualized through Grafana dashboards.
The platform captures infrastructure telemetry such as CPU usage, memory utilization,
network latency, and service response times.

These metrics form the data foundation used by the AI engine to detect anomalies
and recommend automated infrastructure optimizations.
